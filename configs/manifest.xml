<manifest version="2.0" type="device">
    <!-- vendor.lineage.livedisplay and vendor.lineage.touch are intentionally not
         declared: their services are only built for non-tablet devices
         (TARGET_IS_TABLET) and this panel has no mDNIe/KCAL sysfs interface.
         LiveDisplay works through config_setColorTransformAccelerated (DTM). -->
</manifest>
